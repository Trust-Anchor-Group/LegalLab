<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:LegalLab.Converters"
        mc:Ignorable="d"
        x:Class="LegalLab.MainWindow" Title="TAG Legal Lab" Height="450" Width="800" Background="#FFE5E5E5">
    <Grid>
        <TabControl Name="TabControl">
            <TabItem Name="NetworkTab" Header="Network ID">
                <StackPanel Orientation="Horizontal" Background="#FFE5E5E5" Height="Auto">
                    <ScrollViewer>
                        <StackPanel Orientation="Vertical" Width="250" Margin="10">
                            <Label Padding="0">XMPP Server:</Label>
                            <TextBox Name="XmppServer" Text="{Binding XmppServer}" IsEnabled="{Binding ConnectOnStartup, Converter={conv:Not}}"/>
                            <Label Padding="0,10,0,0">Account:</Label>
                            <TextBox Name="XmppAccount" Text="{Binding Account}" IsEnabled="{Binding ConnectOnStartup, Converter={conv:Not}}"/>
                            <Label Padding="0,10,0,0">Password:</Label>
                            <PasswordBox Name="XmppPassword" PasswordChanged="XmppPassword_PasswordChanged" IsEnabled="{Binding ConnectOnStartup, Converter={conv:Not}}"/>
                            <CheckBox Name="CreateAccount" Margin="0,10,0,0" Content="Create Account, if none exists." IsChecked="{Binding CreateAccount}" IsEnabled="{Binding ConnectOnStartup, Converter={conv:Not}}"/>
                            <Label Padding="0,10,0,0" Visibility="{Binding CreateAccount, Converter={conv:BooleanToVisibility}}">Repeat Password:</Label>
                            <PasswordBox Name="XmppPassword2" PasswordChanged="XmppPassword2_PasswordChanged" Visibility="{Binding CreateAccount, Converter={conv:BooleanToVisibility}}" IsEnabled="{Binding ConnectOnStartup, Converter={conv:Not}}"/>
                            <Label Padding="0,10,0,0" Visibility="{Binding CreateAccount, Converter={conv:BooleanToVisibility}}">API Key (for creating accounts):</Label>
                            <TextBox Name="ApiKey" Text="{Binding ApiKey}" Visibility="{Binding CreateAccount, Converter={conv:BooleanToVisibility}}" IsEnabled="{Binding ConnectOnStartup, Converter={conv:Not}}"/>
                            <Label Padding="0,10,0,0" Visibility="{Binding CreateAccount, Converter={conv:BooleanToVisibility}}">API Key Secret (for creating accounts):</Label>
                            <PasswordBox Name="ApiKeySecret" Visibility="{Binding CreateAccount, Converter={conv:BooleanToVisibility}}" PasswordChanged="ApiKeySecret_PasswordChanged" IsEnabled="{Binding ConnectOnStartup, Converter={conv:Not}}"/>
                            <CheckBox Name="TrustServer" Margin="0,10,0,0" Content="Trust server certificate." IsChecked="{Binding TrustServerCertificate}" IsEnabled="{Binding ConnectOnStartup, Converter={conv:Not}}"/>
                            <CheckBox Name="AllowInsecureAuthentication" Margin="0,10,0,0" Content="Allow insecure authentication." IsChecked="{Binding AllowInsecureAlgorithms}" IsEnabled="{Binding ConnectOnStartup, Converter={conv:Not}}"/>
                            <CheckBox Name="StorePassword" Margin="0,10,0,0" Content="Store password instead of digest." IsChecked="{Binding StorePasswordInsteadOfDigest}" IsEnabled="{Binding ConnectOnStartup, Converter={conv:Not}}"/>
                            <Button Name="Connect" Margin="10,10,10,0" Command="{Binding Connect}" Visibility="{Binding ConnectOnStartup, Converter={conv:NotBooleanToVisibility}}">Connect</Button>
                            <Button Name="Disconnect" Margin="10,10,10,0" Command="{Binding Disconnect}" Visibility="{Binding ConnectOnStartup, Converter={conv:BooleanToVisibility}}">Disconnect</Button>
                            <Button Name="RandomizePassword" Margin="10,10,10,0" Command="{Binding RandomizePassword}" IsEnabled="{Binding ConnectOnStartup, Converter={conv:Not}}">Randomize Password</Button>
                        </StackPanel>
                    </ScrollViewer>
                    <ListView Name="Sniffer" Width="Auto">
                    </ListView>
                </StackPanel>
            </TabItem>
            <TabItem Header="Legal ID">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Wallet">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Contracts">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Events">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Script">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        </TabControl>
        <StatusBar Name="StatusBar" VerticalAlignment="Bottom">
            <StatusBarItem Name="XmppState" Content="{Binding State}"/>
            <StatusBarItem Name="Status"></StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
